# Demo walkthrough

完整 demo 已合并到 [README §完整走读](../README.md#完整走读)。这里只保留指向。

> 之前这份文档单独维护了一版用 `examples/basic` fixture 的 6 步走读。和 README 里的"2 分钟上手"路径不一致（一个用 fixture、一个用 `forge new`），新用户容易混淆。统一到 README 之后，这份文件保留作占位。

---

*English version: see [README.en.md §Full walkthrough](../README.en.md#full-walkthrough).*
